/** Exact offline Hybrid draft banner (UX-DR18 / FR-19). */
export const OFFLINE_DRAFT_BANNER =
    'Saved on this device — not on the Evidence Base until you reconnect';

/** Pupil identity conflict on flush — server wins; local draft retained. */
export const PUPIL_IDENTITY_CONFLICT_MESSAGE =
    'The Pupil on the server no longer matches this draft. The server wins — your draft stays on this device so you can resolve it.';
